// To find mean(avg), median(sort and find middle elemnt), 
// mode(Which number is having more number of duplications)
#include<stdio.h>
int main(){
    int n;
    printf("Enter size of array : ");
    scanf("%d",&n);
    int a[n];
}